# vertical-timeline

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build vertical-timeline` to build the library.

## Running unit tests

Run `nx test vertical-timeline` to execute the unit tests via [Jest](https://jestjs.io).

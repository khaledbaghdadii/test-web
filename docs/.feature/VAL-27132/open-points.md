# VAL-27132 — Open Points (to revisit)

Deferred items that are intentionally NOT done in the current implementation and must be
checked/resolved in a follow-up.

| # | Open point | Context | Owner / next action |
|---|------------|---------|---------------------|
| 1 | **Contract (pact) tests for the migrated executor services** | The legacy `BuildAndTestProcessExecutorService` and `BackportProcessExecutorService` (in `features/business-process`) had **no** `*.pact.spec.ts` — only unit specs. They were migrated to new-arch `business-process/data-access` **without** contract tests, because authoring new pacts would require inventing provider-state names (explicitly to be avoided). | Verify on the **provider** side whether these execute endpoints have real provider states; if so, add one `*.pact.spec.ts` **per service** with provider states copied verbatim from the provider. Do NOT hallucinate provider states. |
| 2 | **Contract (pact) tests for the migrated infra-group list + project-users search + user-story validation services** | The legacy `InfraGroupsService` (`features/infra-management`), `ProjectUsersFetcherService`/`ProjectUsersService` (`features/user-management`) and `ValidateUserStoryService` (legacy user-story input) — whose list/search/validate endpoints were copied verbatim into new-arch `data-access` for the infra-group selector, recipients multiselect + user-story input — have **no** `*.pact.spec.ts` on the legacy side. Verified 2026-06-30 (Step 11 pass): no legacy pact files reference `infra/registry/groups`, `user-management/projects/{id}/users`, or `executions/ci-process/validate/user-story`. Migrated with unit specs only; no pacts authored (no provider states to copy). | Verify the provider states for `GET …/projects/{projectId}/infra/registry/config`, the list/`filter`-groups endpoints, `GET …/user-management/projects/{projectId}/users`, and `POST …/executions/ci-process/validate/user-story`; if they exist, add one `*.pact.spec.ts` **per service**, provider states copied verbatim. Do NOT hallucinate. |
| 3 | **`inputAccessMode` is per-field hardcoded in legacy, NOT derived from `providedInputs`** | Decision #5 asked to derive the `providedInputs → ACCESS_*` mapping from legacy. Finding (Step 11 pass): legacy does **not** compute the mode from `providedInputs`. `DefinitionInputComponent.shouldShow` (`features/business-process/.../definition-input`) consumes an `inputAccessMode` that is **authored statically per field/group in each executor template** (`execute-build-and-test-process-input.component.html`, validation repusher inputs, etc.). The migrated `shouldShowInForm`/`isInputEmpty`/`isPrefilled` util (committed) faithfully replicates the `shouldShow` boolean; the **mode assignment** belongs to the executors. The exact Build & Test mapping is recorded in `implementation-log.md` (this pass) for Steps 9/10 to replicate verbatim. | Steps 9/10 executors: assign each migrated field its legacy `inputAccessMode` exactly per the implementation-log table (no invented mapping). |
| 4 | **Step 8 dialog "executor" page is a placeholder** | The Build & Test templates dialog declares Page 2 (`mxevolveMultiPageDialogPage="executor"`) but currently renders only the selected template name. Steps 9/10 must project the real per-family Reactive-Forms executor there, choosing Build & Test vs Backport by `selected().sourceDefinitionId === "on-demand-backport"`, and wiring the reused `scm/widget` reviewers + `test/widget` scenario-definition widgets + the Step 11 selectors. | Steps 9/10: replace the placeholder `<p>` with the BT/Backport executor; the dialog header already binds to the selected definition name. |
| 5 | **Step 8 spec opens the dialog via the public `open()` method (not a DOM click)** | Decision (Step 8 finalize, 2026-06-30): `BuildAndTestTemplatesDialogComponent` exposes no internal DOM trigger — the parent Step-7 landing page calls `open()` via a template ref. The spec therefore calls `componentInstance.open()` as the public entry point. A host-template/DOM-button trigger was rejected because the service is a **component-level** provider (`providers: [BusinessProcessDefinitionService]`) that can only be mocked by overriding `componentProviders` on a **direct** render, which is incompatible with a host wrapper. The multi-page-dialog shell is kept **real** (it projects pages via `contentChildren`; mocking it would hide all page DOM); only the `mxevolve-single-select-dropdown` child is mocked with `MockComponent`. | No action required — documented design choice. If Step 7 later exposes a Build button bound to this component, prefer driving open() through that DOM button in an integration-level spec. |
| 6 | **Do NOT run `:web:spotlessApply` on this branch** | Spotless is `ratchetFrom("origin/main")` with Prettier **defaults** (printWidth 80), but the committed branch style is **printWidth 120 + double quotes**. Running `:web:spotlessApply` reformats **40+ already-committed files across the whole branch** (line re-wrapping) — large out-of-scope churn. Observed and reverted during the Step 8 finalize. | Format new web files by hand to **double quotes / printWidth 120 / long single-line exports**. If a true repo-wide reformat is ever wanted, do it as its own dedicated commit, not inside a feature step. |
| 7 | **`mxevolve-notifications-recipients-input` exposes a `formControl` INPUT, not a `ControlValueAccessor`** | Discovered in Steps 9/10 (batch 3b): the Step-11 notifications widget declares `formControl = input.required<FormControl>()`. Binding `[formControl]` to it activates Angular's `FormControlDirective`, which **throws `NG01203` ("No value accessor")** in Angular 21 — proven empirically by rendering the real widget. The legacy executors used the identical pattern but never rendered it in a spec, so the breakage was latent. Steps 9/10 consume the widget via a new `mxevolveNoopValueAccessor` directive (`composite-widget/.../shared/noop-value-accessor.directive.ts`) that supplies a no-op `NG_VALUE_ACCESSOR` on the element; the widget keeps driving the bound control through its own input (verified `setValue` round-trips). Cannot cherry-pick out `FormControlDirective` because `FormGroupDirective`/`FormControlName` are **not standalone** — `ReactiveFormsModule` must be imported whole. | Step-11 follow-up: rename the widget's input to `notificationsRecipientsFormControl` (mirroring `infraGroupFormControl` / `reviewersFormControl`) **or** make it a real `ControlValueAccessor`, then drop the noop-accessor workaround. Steps 15/19 must apply `mxevolveNoopValueAccessor` for any notifications usage in the meantime. |
| 8 | **BT repository / config-branch editable fallbacks + backport prefilled-missing behaviour** | (a) The Step-11 set has no new-arch repository or config-branch *selector* widgets, so the BT executor renders `repositoryId` / `configurationBranchName` / `configurationParentBranch` as plain text inputs for the (non-occurring) case where a definition does not prefill them. For real Build & Test definitions these are always prefilled → hidden in the form → shown read-only in the details panel, so the fallback never surfaces. (b) Legacy backport `getInputValue` **threw** when a prefilled input (`repositoryId`/`mergeConfigurationId`/`buildAndTestInfraGroup`) was missing; the new backport executor returns `""` instead of throwing (always present for real backport definitions). | If a non-prefilled repository/branch case ever becomes real, migrate the legacy repository/config-branch selectors to `business-process/widget` and wire them. Confirm backend rejects empty prefilled values gracefully (toast) if (b) ever occurs. |

> Decision source: developer confirmation on 2026-06-30 ("migrate them with no test but add
> somewhere as open points still to check their contract tests").
---

## Batch 4a additions (Steps 12/13/14)

| # | Open point | Context | Owner / next action |
|---|------------|---------|---------------------|
| 9 | **Official Status & BP Quality Level render as plain text in the new Validation table** | The legacy validation table renders Official Status via the styled `mxevolve-business-process-official-status` badge component; the new ag-grid Validation table shows the raw `officiality` / `businessProcessQualityLevel` field values as plain text (new design language, no new-arch badge cell exists yet). The **data value and the column filters are preserved** (full parity); only the visual badge styling differs. | If the styled badge is desired, add a new-arch official-status / quality-level cell renderer (mirroring `ExecutionStatusTagComponent`) and set it as the column `cellRenderer`. |
| 10 | **Status column filter is now functional (intentional deviation from the BT mirror)** | The BT activity (Step 6) declared a `statuses` column filter but its query mapper read only `req.statuses` (the active/history split), leaving `filters.statuses` **unread** — so the BT Status filter is effectively dead. The Validation mapper (`resolveStatuses`) intersects the picked statuses with the table's status set, making the Status filter functional and scoped (Active never leaks history). Decided per "choose the best option and proceed". | Consider back-porting the same `resolveStatuses` behaviour to the BT activity for consistency, or confirm the BT Status filter is meant to be inert. |
| 11 | **Validation dialog reuses BT's `SubFamilyOptionsDataProvider` via a cross-folder relative import** | `validation-templates-dialog.component.ts` imports `SubFamilyOptionsDataProvider`/`SubFamilyOptionsParams` from `../../build-and-test/templates-dialog/...` (same `composite-widget` lib) rather than duplicating the adapter — it is generic (adapts already-derived options to the single-select dropdown's frontend-state contract). Couples the Validation dialog to the BT folder path. | If the BT templates-dialog folder is ever moved/removed, relocate `sub-family-options-data-provider.ts` to `composite-widget/src/lib/shared/` (next to `derive-sub-families.ts`) and update both dialogs. |
| 12 | **Validation activity loads ALL definitions to populate two filter dropdowns** | `ValidationActivityComponent` calls `getBusinessProcessDefinitions({ projectId })` (no `extendable`/`executable`) once via `rxResource` to feed the Business Process Definition + Process Name multiselect options and to resolve Process Name → `definitionIds` (parity with the legacy `BusinessProcessDefinitionFilterResolverService`, replicated as the pure `resolveValidationDefinitionIds`). This is one extra page-load request (no N+1). | None required (matches legacy behaviour). If the definitions list is large, consider server-side option lookup. |

> Batch 4a decisions taken autonomously (developer unavailable mid-run): plain-text Official Status / BP
> Quality Level (#9), functional Status filter (#10), reuse BT's options provider (#11), and load all
> definitions for the data-derived filters (#12).

---

## Batch 4b additions (Step 15 — Validation executor)

| # | Open point | Context | Owner / next action |
|---|------------|---------|---------------------|
| 13 | **Validation config-param + test-scenario + scope-commit fields rendered as plain fallback inputs (no rich self-fetching widgets)** | Decision #2 asked to rebuild the validation-specific config-param / scope-commit inputs fresh on the shared `mxevolve-single-select-dropdown` / `mxevolve-multiselect-dropdown`. The legacy config-params (`ValidationProcessConfigurationParametersComponent` with MQG/DQG sub-forms, final-product fetcher) and scope-commit (`ValidationScopeStartCommitIdInputComponent` with `rtp-commit-id-data-provider` hitting the commits endpoint) are large self-fetching component trees needing new-arch data-access services not yet present. For **real** validation definitions these config/test fields are **always prefilled** → hidden from the form → shown read-only in the "{name} Details" panel (`mxevolve-validation-prefilled-inputs`), so the editable controls never surface — identical situation to BT open-point #8. The Step-15 executor therefore renders them as plain `pInputText` / radio fallbacks (repository, BP quality level, createBranch, archival/parent branch, final product, RTP/config commit) and the conditional `validationScopeStartCommitId` as a plain text commit-id input. Payload, validators and the scope-commit visibility matrix are reproduced exactly; only the rich pickers are deferred. | If a non-prefilled validation case ever becomes real, migrate the legacy config-params + scope-commit (commit/final-product/repository) selectors to `business-process/widget` and wire them on the shared dropdowns. |
| 14 | **`qualityGateScenarioDefinitionIds` (multi) has no editable new-arch widget** | The reused `test/widget` `ScenarioDefinitionDropdownComponent` is a **single-select** CVA (`string \| null`), but validation's `qualityGateScenarioDefinitionIds` is a **multi** (`string[]`, seeded from `providedInputs.testScenarioIds`). No new-arch multi-select scenario widget exists. For real validation definitions this field is prefilled → hidden → shown read-only in the details panel ("Quality Gate Test Scenarios"), so no editable control is rendered (the seeded value is still submitted). | Build a multi-select scenario-definition widget in `test/widget` (or extend the existing one) and wire it for the (non-occurring) non-prefilled case. |
| 15 | **`parentBranchName` is an optional control → never shown as an editable form field** | Legacy `parentBranch` carries **no validators** (optional). Under the resolved architecture (decision #4: render only `shouldShowInForm` non-prefilled fields with `ACCESS_INVALID_INPUTS_ONLY`), an optional control is never `invalid`, so it is never rendered editable; it appears read-only in the details panel only when prefilled. In legacy the parent branch was surfaced inside the MQG/DQG config-params sub-forms (out of scope here). Functionally harmless: the parent branch still drives the scope-commit visibility (via its value) and is submitted from its prefilled/seed value. | If editable parent-branch selection is required for the create-branch flow, migrate the legacy MQG/DQG config-params sub-forms to `business-process/widget`. |
| 16 | **No contract (pact) test for `ValidationProcessExecutorService` or the new `DevelopmentService.getDevelopments` (list)** | Verified 2026-07-01: the legacy `ValidationProcessExecutorService` (`features/business-process/.../validation-process-definition-executor`) has only a **unit** spec (`validation-process-executor.service.spec.ts`), **no** `*.pact.spec.ts`; and the legacy `ScmManagementService.getDevelopments` list endpoint (`GET …/scm-management/projects/{id}/developments?repositoryId=&name=`) has **no** legacy pact (`features/scm` has zero `*.pact.spec.ts`; the new-arch `web-scm-management-service.pact.spec.ts` covers only `getDevelopment` **singular**). Both were migrated/added with unit specs only; no pacts authored (no provider states to copy). The existing singular-`getDevelopment` pact is **unaffected** (purely additive change — new method + new `Developments`/`DevelopmentFilters` types, the singular method/types untouched). | Verify on the provider side whether `POST …/executions/master-validation/execute` and the `GET …/developments` list endpoint have real provider states; if so, add one `*.pact.spec.ts` **per service**, provider states copied verbatim. Do NOT hallucinate. |

> Batch 4b decisions taken autonomously (developer unavailable mid-run): plain fallback inputs for the
> always-prefilled config/test/scope-commit fields (#13), no editable multi-select scenario widget (#14),
> optional parent-branch shown read-only only (#15), and no authored pacts where no legacy pact exists (#16).

---

## Batch 5a additions (Steps 16/17/18 — Upgrade)

| # | Open point | Context | Owner / next action |
|---|------------|---------|---------------------|
| 17 | **Upgrade Official Status / BP Quality Level and the 4 `input.*` phrase columns render as plain text** | Same visual deviation as #9 (Validation): the legacy upgrade table renders Official Status via the styled `mxevolve-business-process-official-status` badge and shows the nested `input.*` values (BP Quality Level, Parent MX Archival Branch, MX Version, MX Build ID, Configuration Branch Name) via tooltip'd cells. The new ag-grid Upgrade table shows Status as the shared `ExecutionStatusTagComponent` badge but renders Official Status + the `input.*` fields as plain text (ag-grid dot-path `field`). **Data values, column order and all column filters are preserved** (full field-by-field parity); only badge/tooltip styling differs. | If the styled badges/tooltips are desired, add new-arch official-status / quality-level cell renderers (mirroring `ExecutionStatusTagComponent`) and set them as the column `cellRenderer`. |
| 18 | **Days Extended + Duration legacy pipes reproduced inline (not imported)** | The legacy upgrade table formats Days Extended with `@mxflow/pipe` `DaysCountPipe` ("N Day(s)" / "-") and Duration with `DurationPipe`/`DurationFormatterPipe` ("{h}h {m}m {s}s"). These pipes live in the legacy `@mxflow/pipe` lib (not on the services-only `@mxflow/*` eslint allowlist). Rather than import them, the Upgrade activity reproduces both formats inline (`formatDaysExtended` valueFormatter + `durationValue` valueGetter), byte-faithful to the legacy output. Consistent with the Steps 9/10 inline-validator reproduction (change #7). | If a shared new-arch duration/days pipe is later added, swap the inline helpers for it. No behaviour change expected. |
| 19 | **Upgrade templates dialog `executor` page is a placeholder** | The Upgrade templates dialog (Step 18) declares Page 2 (`mxevolveMultiPageDialogPage="executor"`) but currently renders only `<p>{{ definition.name }}</p>` (analogous to Validation open-point #4 before Step 15). No `created` output / `onExecutorCreated` is wired yet. | Step 19 must project the real upgrade Reactive-Forms executor there, add the `created` output + `onExecutorCreated()` (close + emit) and bind `(created)` on the executor element — mirror the Step-15 Validation dialog wiring exactly. |
| 20 | **No NEW contract (pact) test for Step 16 — existing upgrade-listing pact reused** | Step 16 reused `UpgradeProcessListingService` **unchanged** (no endpoint copied/added). That service already has a pact (`data-access/.../contracts/upgrade-process/upgrade-process-listing-service.pact.spec.ts`, GET `executions/binary-upgrade`), which is **unaffected**. Per the contract-test rule (only copy a pact when copying/adding an endpoint), no new pact was authored and none is owed. Verified 2026-07-01. | None required — the existing listing pact covers the reused endpoint. |

> Batch 5a decisions taken autonomously (developer unavailable mid-run): plain-text Official Status +
> `input.*` columns (#17), inline reproduction of the Days Extended / Duration pipes (#18), placeholder
> executor page pending Step 19 (#19), and reuse of the existing upgrade-listing pact with no new pact
> authored (#20).

---

## Batch 5b additions (Step 19 — Upgrade executor, Page 2 + prefilled display)

| # | Open point | Context | Owner / next action |
|---|------------|---------|---------------------|
| 21 | **No contract (pact) test for the migrated `UpgradeProcessDefinitionExecutorService`** | Verified 2026-07-01: the legacy `UpgradeProcessDefinitionExecutorService` (`features/business-process/.../upgrade-process/service`) has only a **unit** spec, **no** `*.pact.spec.ts`; no legacy pact references `POST …/executions/binary-upgrade/execute`. Migrated to new-arch `data-access` (`upgrade-process/executor/`) with a unit spec only; no pact authored (no provider states to copy). | Verify on the provider side whether `POST …/executions/binary-upgrade/execute` has real provider states; if so, add one `*.pact.spec.ts` for the service, provider states copied verbatim. Do NOT hallucinate. |
| 22 | **Upgrade executor self-provides `UpgradeProcessDefinitionExecutorService` (component-level provider); BT/Validation executors do not** | Per the Step-19 instruction (`providers: [UpgradeProcessDefinitionExecutorService]`) and the legacy modal (which provided the service at the component), the new upgrade executor lists the service in its `@Component.providers` — production-correct (the component owns its own executor service instance). The committed BT/Validation executors instead **inject** their `@Injectable()` executor service without self-providing (they rely on ancestor/root provision, which appears absent). The upgrade spec mocks the service via Testing Library `componentProviders` (component-level override) rather than `providers`. | Confirm the BT/Validation executor services are actually provided at runtime (they may throw `NullInjectorError` if no ancestor provides them); if not, add `providers: [...ExecutorService]` to those two executors for consistency with the upgrade executor. |
| 23 | **Factory-product (object) + multi-select test-scenario upgrade fields render as read-only notes when non-prefilled (no editable rich picker)** | Decision #2 offered "rebuild fresh on the shared dropdowns OR render as plain fallback inputs like the Validation executor". The legacy `factoryProduct` / `referenceFactoryProduct` (object pickers), `testScenarioIds` (multi-select scenario picker) and the `upgradeJump` / `referenceEnvironmentDefinitionId` selectors are large self-fetching component trees with no new-arch widget yet. For **real** upgrade definitions these are **always prefilled** → hidden from the form → shown read-only in the "{name} Details" panel (`mxevolve-upgrade-prefilled-inputs`, with the two factory products expanded into MX/BIP sub-attribute rows), so the editable controls never surface — identical to Validation #13/#14 and BT #8. The executor therefore renders the two factory-product fields and the multi-select test-scenario field as informational read-only notes (the seeded object/array value is still submitted intact), and the other selectable string fields as plain `pInputText` fallbacks; the three infra-group fields use the reused `mxevolve-business-process-infra-group-selector`, and notifications the reused `mxevolve-notifications-recipients-input` (+ `mxevolveNoopValueAccessor`). Payload, validators and field/group parity are reproduced exactly; only the rich editable pickers are deferred. | If a non-prefilled upgrade case ever becomes real, migrate the legacy factory-product / upgrade-jump / environment-definition / multi-select scenario selectors to `business-process/widget` (on the shared dropdowns) and wire them. |
| 24 | **"All groups always rendered" (step field-fidelity note) interpreted as legacy behaviour; the new executor gates empty groups** | The Step-19 field-fidelity note says "All groups always rendered" (describing the legacy template, which always renders every `definition-input-group`). CRITICAL decision #2 ("match the Validation executor's approach for consistency") supersedes: the committed BT/Validation executors gate each group with an `@if (showXGroup())` that hides a group when all its fields are prefilled/hidden. The upgrade executor follows the same pattern (`showMxGroup` / `showConfigurationGroup` / `showInfrastructureGroup` / `showTestsGroup` / `showReferenceEnvironmentGroup`), so a fully-prefilled definition shows only Official Status + Execution Name (+ Notifications when recipients are empty), and every prefilled value still appears in the details panel. No field or group value is lost. | None required — consistent with the committed BT/Validation executors. If literal "always render every group heading" is desired, remove the `showXGroup` guards. |

> Batch 5b decisions taken autonomously (developer unavailable mid-run): no authored pact where no legacy
> pact exists (#21), component-level provision of the upgrade executor service with a `componentProviders`
> spec override (#22), read-only notes for the always-prefilled factory-product / multi-select scenario
> fields (#23), and group gating consistent with the committed BT/Validation executors rather than
> always-rendered empty group headings (#24).

/**
 * Business-process-execution MFE path segments, duplicated into this util so the
 * URL factory stays a pure, dependency-free helper (mirrors the values in
 * `libs/config/src/lib/mfe-urls`).
 */
export const BINARY_UPGRADE_MFE_PATH = "upgrade-processes";
export const CI_PROCESS_MFE_PATH = "build-and-test-processes";
export const MASTER_VALIDATION_MFE_PATH = "validation-processes";
